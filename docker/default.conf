agents {
  spans {
    enabled = true
    port = 34000
    dispatchers {
      kafka {
        bootstrap.servers = "haystack-test-kafka.test.monitoring.expedia.com:9092"
        producer.topic = "proto-spans"
        buffer.memory = 1048576
        retries = 2
      }
    }
  }
  blobs {
    enabled = true
    port = 34001
    max.blob.size.in.kb = 512
    dispatchers {
      s3 {
        keep.alive = true
        max.outstanding.requests = 150
        should.wait.for.upload = true
        max.connections = 50
        retry.count = 1
        bucket.name = "haystack-blobs"
        region = "us-east-1"
        aws.access.key = "accessKey"
        aws.secret.key = "secretKey"
      }
    }
  }
}